import { OrdfilterPipe } from './ordfilter.pipe';

describe('OrdfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
