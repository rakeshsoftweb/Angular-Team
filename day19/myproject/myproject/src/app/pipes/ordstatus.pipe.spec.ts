import { OrdstatusPipe } from './ordstatus.pipe';

describe('OrdstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
